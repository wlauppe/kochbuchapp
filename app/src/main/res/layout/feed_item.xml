<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>
        <variable
            name="feedViewModel"
            type="de.psekochbuch.exzellenzkoch.userinterfacelayer.viewmodel.FeedViewModel" />
        <variable
            name="value"
            type="String" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/textView_recipe_name_feed"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@{value}" />

        <Button
            android:id="@+id/button_goto_feed_recipe"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="öffnen" />
    </LinearLayout>
</layout>